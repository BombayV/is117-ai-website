#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Validating commit message format..."
bunx commitlint --edit "$1" || { echo "Commit message validation failed"; exit 1; }
echo "Commit message format is valid!"
